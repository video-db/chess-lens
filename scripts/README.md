# Scripts

This folder contains small operational scripts used by development and installation flows.

Install scripts bootstrap Chess Lens on supported platforms. `delay-electron.js` coordinates the dev workflow by waiting for the renderer server before launching Electron.
