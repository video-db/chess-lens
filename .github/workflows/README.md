# Workflows

This folder contains GitHub Actions workflow definitions for repository automation.

The current workflow builds and validates the application in CI. Keep workflow YAML here when it should run on GitHub events such as pushes, pull requests, or releases.
