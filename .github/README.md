# GitHub Configuration

This folder holds GitHub-specific repository configuration.

Use `workflows/` for CI and release automation. Add other GitHub metadata here only when it is consumed by GitHub itself, such as issue templates, pull request templates, or dependency configuration.
