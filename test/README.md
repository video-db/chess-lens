# Tests

This folder contains unit and smoke-support tests that used to live beside source files.

The folder mirrors the source tree without the `src/` prefix, so `test/main/lib/chess/foo.test.ts` covers `src/main/lib/chess/foo.ts`.
