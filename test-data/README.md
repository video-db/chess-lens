# Test Data

This folder contains local fixtures for manual tests, benchmark scripts, and regression checks.

Use `fixtures/project-frames/` for captured chessboard images and expected-position fixtures. Keep synthetic or recorded inputs here when they are needed by tools but should not live inside production source code.
