# Source

This folder contains the application source code.

`main/` is the Electron main process, `preload/` exposes safe bridge APIs, `renderer/` contains React UI, `shared/` holds cross-process contracts, and `types/` contains ambient TypeScript declarations.
