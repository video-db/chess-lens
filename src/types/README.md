# Ambient Types

This folder contains project-wide TypeScript declaration files.

Use it for ambient module declarations, global types, asset import typing, and external SDK types that do not naturally belong to one feature area.
