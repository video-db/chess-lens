# Utilities

This folder contains small main-process utility helpers.

The current encryption helper supports secure handling of local secrets and tokens. Keep narrowly scoped, dependency-light helpers here when they do not belong to a larger service or library module.
