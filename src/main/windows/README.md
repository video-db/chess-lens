# Windows

This folder contains Electron window factory and management code.

The widget window module owns creation and behavior for the floating coaching overlay. Add new files here when a feature needs a distinct BrowserWindow lifecycle.
