# Chess Screenshot Helpers

This folder contains helper code for `chess-screenshot.service.ts`.

It keeps screenshot polling constants and FEN guard utilities close to the capture pipeline without crowding the main services folder.
