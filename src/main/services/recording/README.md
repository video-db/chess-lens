# Recording Services

This folder contains recording export and recovery services.

It handles export polling, markdown export, recording export status recovery, and startup recovery for sessions that completed while the app was closed.
