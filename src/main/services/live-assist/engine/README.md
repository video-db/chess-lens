# Live Assist Engine

This folder contains helpers for presenting chess-engine output to the widget and postgame pipeline.

It tracks engine state, formats engine summaries, describes best-move pieces, and builds win-probability snapshots.
