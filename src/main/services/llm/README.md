# LLM Services

This folder contains the main-process LLM integration.

It keeps request orchestration, DTO types, and OpenAI message/tool conversion together so feature services can call the LLM layer without owning provider-specific formatting.
