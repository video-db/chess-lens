# Config

This folder contains static configuration data used by the Electron main process.

The MCP server template registry lives here so setup screens and connection services can share the same known server definitions.
