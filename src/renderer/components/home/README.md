# Home Components

This folder contains the app home view.

The home view is the landing surface inside the desktop app after startup or authentication. Keep top-level dashboard and entry-state UI here when it does not belong to a more specific feature folder.
