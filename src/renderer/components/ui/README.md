# UI Components

This folder contains reusable low-level UI primitives.

Buttons, cards, dialogs, inputs, labels, tabs, tooltips, brand marks, and similar building blocks live here. Feature folders should compose these primitives rather than redefining shared UI patterns.
