# Components

This folder contains React component families for the main renderer app.

Feature folders such as `recording/`, `history/`, `meeting-setup/`, `copilot/`, `auth/`, and `settings/` hold domain UI. Shared app shell and design-system primitives live in `layout/` and `ui/`.
