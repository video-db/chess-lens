# Auth Components

This folder contains authentication and setup screens.

Components here handle API-key entry, account connection, calendar setup, and initial recording preferences. They are renderer-only UI and should call app APIs through hooks, stores, or `src/renderer/api/`.
