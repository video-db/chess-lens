# Hooks

This folder contains renderer React hooks.

Hooks coordinate UI state with stores, permissions, session lifecycle events, MCP state, live assist, copilot data, and global recorder events. Keep reusable React-side behavior here when it is not a visual component.
