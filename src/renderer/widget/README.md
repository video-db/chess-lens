# Widget Renderer

This folder contains the renderer entrypoint and state for the floating overlay window.

The widget is a compact always-on-top interface for live coaching. It has its own React app, store, and components so overlay behavior can evolve independently from the main renderer.
