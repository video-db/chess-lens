# Styles

This folder contains global renderer styles.

`globals.css` defines Tailwind layers and app-wide styling defaults. Component-local styling should stay with components when possible, while global tokens and resets belong here.
