# Renderer

This folder contains the React renderer applications.

The main desktop UI starts from `main.tsx`, `App.tsx`, and `index.html`. Feature UI is organized under `components/`, shared state under `stores/`, transport adapters under `api/`, hooks under `hooks/`, and the separate floating overlay under `widget/`.
