# Renderer Assets

This folder contains static assets imported by renderer UI.

Use it for icons, short media, and other assets bundled directly into React screens. Packaging-level resources belong in the top-level `resources/` folder.
