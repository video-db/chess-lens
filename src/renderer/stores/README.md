# Stores

This folder contains renderer state stores.

Zustand stores here hold app state for chat, config, copilot, live assist, MCP, meeting setup, sessions, transcription, and visual index features. Keep persistent or cross-component renderer state here rather than inside unrelated components.
