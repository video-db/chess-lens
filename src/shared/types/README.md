# Shared Types

This folder contains TypeScript types shared between the main process, preload layer, and renderer.

Use these files for domain contracts that need to stay aligned across process boundaries, including API, calendar, capture, IPC, live assist, MCP, meeting setup, and workflow types.
