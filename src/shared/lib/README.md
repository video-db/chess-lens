# Shared Libraries

This folder contains process-neutral helper logic.

Use it for pure utilities that can run in both main and renderer contexts. The current move classification helper supports chess analysis presentation without depending on Electron APIs.
