# Shared Config

This folder contains configuration shared across processes.

Game coaching configuration lives here so both main-process logic and renderer UI can agree on chess coaching behavior without duplicating constants.
