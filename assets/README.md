# Assets

This folder contains application assets that are not renderer UI source files.

Use `models/` for runtime model files such as ONNX weights. Static images and videos that are directly imported by React components usually belong under `src/renderer/assets/` instead.
